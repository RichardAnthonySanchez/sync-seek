export default function ViewStopFetch() {
  const button = document.createElement("button");
  button.id = "view-stop-fetch";
  button.innerHTML = "stop fetch";
  document.body.appendChild(button);
}
