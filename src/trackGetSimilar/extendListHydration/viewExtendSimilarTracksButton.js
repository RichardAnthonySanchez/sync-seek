export default function ViewExtendSimilarTracksButton() {
  const button = document.createElement("button");
  button.id = "extend-similar-tracks";
  button.innerHTML = "extend list";
  document.body.appendChild(button);
}
