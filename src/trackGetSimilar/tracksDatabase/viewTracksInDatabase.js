export default function ViewTracksInDatabase() {
  const button = document.createElement("button");
  button.id = "view-tracks-in-database";
  button.innerHTML = "view tracks in database";
  document.body.appendChild(button);
}
