export default function ViewMatchingTracks() {
  const button = document.createElement("button");
  button.id = "view-matching-tracks";
  button.innerHTML = "view matching tracks";
  document.body.appendChild(button);
}
